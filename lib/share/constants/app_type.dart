enum ThemeMode {
  dark,
  light,
}

enum AuthStatus { loggedIn, notLoggedIn }

enum ButtonType {
  primary,
  secondary,
  ghost,
  link,
  round,
  outline,
}

enum LinearType { container, primary }

enum SnackbarType {
  error,
  success,
  warning,
}
